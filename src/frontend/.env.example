# Backend API base URL (no trailing slash)
# Leave empty when running behind Nginx reverse proxy (Docker), so the frontend
# calls the same origin and Nginx forwards /api/* to the backend container.
# For local dev outside Docker, point directly to the backend, e.g. https://localhost:10002
VITE_API_BASE_URL=
